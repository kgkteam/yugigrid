# 1) Netlify Functions - NEVER let SPA redirects catch these
/.netlify/functions/*  /.netlify/functions/:splat  200

# 2) Chain route
/chain  /chain.html  200

# 3) SPA fallback (so refresh on /chain or other routes won't break)
/*  /index.html  200
